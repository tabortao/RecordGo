# 2026-01-26 生长记录头像角标移除

## 目标
- 移除「生长记录」卡片头像右下角（头像下方叠加）的状态角标 `div`。

## 影响范围
- 前端：生长记录时间线卡片组件（TimelineCard）。

## 实施步骤
- 定位头像区域的角标 DOM（绝对定位 `absolute -bottom-1 -right-1 ...`）。
- 删除该 DOM 或取消渲染逻辑，确保不影响头像布局与点击区域。

## 验证
- 本地打开「生长记录」页面，确认头像右下角角标不再显示。
- 检查卡片其余信息/布局未发生偏移或遮挡。


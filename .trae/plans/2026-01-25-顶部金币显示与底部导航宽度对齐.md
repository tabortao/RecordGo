# 2026-01-25 顶部金币显示与底部导航宽度对齐

## 目标

- 任务页顶部栏：保持“图标 + 可用金币数字”的展示，并放在任务统计入口图标左侧。
- 底部导航：调整容器宽度，使其与页面顶部栏/内容区域宽度一致，避免宽屏下偏窄。

## 范围

- 前端：任务页顶部栏布局（TasksPage）
- 前端：底部导航容器宽度（BottomNav）

## 方案

- 顶部金币：在顶部栏右侧操作区新增一个金币展示块（Coin 图标 + 数字），并与统计入口图标同一行排列。
- 底部导航宽度：将底部导航安全区容器宽度改为 `calc(100vw - 32px)`，与页面常用 `px-4` 留白一致。

## 验证点

- 任务页顶部右侧：金币图标与数字显示正常，深色/浅色模式颜色可读。
- 金币显示位置：位于统计入口图标左侧，且不挤压标题区域（移动端不换行或可接受的最小换行）。
- 底部导航：宽屏下宽度不再被 900px 限制；与顶部栏左右留白一致。
- 运行检查：`pnpm run lint; pnpm run lint:fix; pnpm run typecheck` 通过。

## 结论（执行后填写）

- [x] 已完成并自测通过
  - 顶部金币：已调整为“Coin 图标 + 数字”，并放在统计入口图标左侧
  - 底部导航：已取消 900px 限制，宽度与页面常用左右留白保持一致

# 2026-01-25 UI修复与朗读默认配置

## 目标

- [x] 关于页进入“使用帮助”后，点击返回应回到关于页
- [x] “我的”页面刷新与加载效果与其它页面一致（避免从上到下突兀刷新）
- [x] 任务页右上角“任务统计”图标使用彩色图标
- [x] 朗读设置-自定义接口：新增默认配置“云扬”

## 实施要点

- 路由返回优先使用 `router.back()`；若无历史栈则回退到 About 页（避免从深链进入时返回到空白）
- 统一各页面的 `el-scrollbar / pull-to-refresh / skeleton`（以项目现有通用组件/样式为准）
- 任务统计图标优先复用现有彩色 icon 资源；若无则引入单个 SVG 资源并保持尺寸一致
- 朗读默认配置写入与现有“默认列表/初始化逻辑”一致，确保字段名与后端/调用处完全匹配

## 验证

- [x] 从关于页进入使用帮助，再点返回，回到关于页且滚动位置/标题正常
- [x] “我的”页进入与下拉刷新时的过渡、骨架、空状态与任务/心愿页一致
- [x] 任务页右上角图标显示为彩色且在深色模式/浅色模式均清晰
- [x] 朗读设置默认出现“云扬”，选择后能正确以 POST 调用并携带音色/语速/语调
